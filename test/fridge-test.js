import sinon from 'sinon'
import assert from 'assert'
import {addIngredient, delIngredient}  from '../modules/fridge'

describe('addIngredient()', function(){
  it('should add ingredient into both session and database', function () {
  })
  it('should throw error if there is duplicate items', function() {

  })
  it('should throw error if session key is missing', function() {

  })
})
describe('delIngredient()', function() {
  it('delete ingredient', function() {

  })
  it('throw error if ingredient doesnt exist', function() {

  })
  it('should throw error if session key is missing', function() {
    
  })
})
